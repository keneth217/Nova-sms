package com.novastack.sms.domain.enums;

/**
 * BUSINESS — ongoing commercial / organizational use (no expiry).
 * EVENT — short-term use for events; active for a limited number of days.
 */
public enum OrganizationAccountType {
    BUSINESS,
    EVENT
}
